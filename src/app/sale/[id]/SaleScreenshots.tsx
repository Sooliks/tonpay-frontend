import React, {useState} from 'react';

const SaleScreenshots = () => {
    const [currentScreenIndex,setCurrentScreenIndex] = useState<number>(0)
    return (
        <div>

        </div>
    );
};

export default SaleScreenshots;